# memory-planner

Static and dynamic buffer-lifetime planning experiments.

This repository is one component in a larger systems/ML-runtime line of work. Interfaces are kept explicit so scheduling, ownership, memory behavior, and profiling remain inspectable.
